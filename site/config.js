export const appConfig = {
  // Fill these with your own Supabase project values.
  // `supabasePublishableKey` is safe to expose in the browser for a static site.
  supabaseUrl: "",
  supabasePublishableKey: "",
  // Legacy fallback. Prefer `supabasePublishableKey` for new projects.
  supabaseAnonKey: "",
  // Leave empty to auto-resolve to the current site's /like.html path.
  githubRedirectTo: "",
};
